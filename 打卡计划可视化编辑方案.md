# 打卡计划可视化编辑方案

## 一、设计目标

将现有的"新建打卡计划"流程改造为**可视化画布编辑器**模式：
- 创建计划只需填写名称，无需立即选择任务
- 在编辑区以**彩色节点 + 箭头连线**的方式编排打卡任务流程
- 支持从任务面板**拖拽**任务到画布、**连线**、**删除**等直观操作
- 编辑完成后一键保存，结构化数据持久化到本地

---

## 二、用户体验流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         用户操作流程                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ① 工作区点击"新建"                                                      │
│     ├─ 弹出轻量弹窗：只需填写"计划名称"                                    │
│     └─ 点击确定 → 创建空计划（无任务节点）                                 │
│                                                                         │
│  ② 工作区点击某个计划                                                     │
│     ├─ 编辑器区域打开"任务编排画布"Tab                                     │
│     ├─ 左侧：可拖拽的任务面板（已安装的任务列表）                            │
│     └─ 右侧：可视化画布                                                   │
│                                                                         │
│  ③ 在画布中编排任务                                                      │
│     ├─ 从左侧拖入任务 → 生成彩色节点                                      │
│     ├─ 拖动节点到合适位置                                                 │
│     ├─ 从节点拖出箭头连接到另一个节点（建立执行顺序）                        │
│     ├─ 选中节点按 Delete / 右键删除                                       │
│     └─ 支持撤销/重做                                                     │
│                                                                         │
│  ④ 保存计划                                                             │
│     └─ 点击"保存" → 结构化数据（nodes + edges）写入 localStorage           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 三、数据结构设计

### 3.1 新增/修改的类型定义

```typescript
// src/renderer/types/checkin.ts

/** 画布上的任务节点 */
export interface PlanNode {
  id: string                    // 节点唯一ID，如 "node-{timestamp}-{seq}"
  taskId: string                // 关联的 CheckinTask.id
  taskName: string              // 任务名称（冗余存储，方便渲染）
  category: string              // 任务分类（用于节点着色）
  x: number                     // 画布坐标 X
  y: number                     // 画布坐标 Y
  completed: boolean            // 是否已完成（执行阶段使用）
  completedAt?: string          // 完成时间
}

/** 节点之间的连线 */
export interface PlanEdge {
  id: string                    // 连线唯一ID
  sourceNodeId: string          // 起始节点ID
  targetNodeId: string          // 目标节点ID
  type: 'main' | 'branch' | 'side'  // 连线类型：主线 / 支线A / 支线B
}

/** 连线类型常量 */
export const EdgeType = {
  MAIN: 'main' as const,
  BRANCH: 'branch' as const,
  SIDE: 'side' as const,
}

/** 打卡计划（改造后） */
export interface CheckinPlan {
  id: string
  name: string
  createdAt: string
  nodes: PlanNode[]             // 替代原来的 tasks: PlanTask[]
  edges: PlanEdge[]             // 新增：节点之间的连线关系
}
```

### 3.2 数据结构对比（旧 vs 新）

| 维度 | 旧方案 | 新方案 |
|------|--------|--------|
| 任务存储 | `tasks: PlanTask[]`（线性数组 + order） | `nodes: PlanNode[]`（含坐标 x/y） |
| 任务关系 | 隐式顺序（按 order 字段） | 显式连线 `edges: PlanEdge[]` |
| 布局方式 | 固定纵向列表 | 自由画布定位 |
| 空计划 | 必须至少选一个任务才能创建 | 允许零节点创建 |

---

## 四、组件架构设计

```
src/renderer/
├── plugins/builtin/components/
│   ├── CheckinPanel.vue              # 工作区面板（改造：简化新建弹窗）
│   ├── CheckinPlanEditor.vue         # 【新增】打卡计划编辑器（主容器）
│   ├── CheckinPlanCanvas.vue         # 【新增】可视化画布组件
│   ├── CheckinTaskPalette.vue        # 【新增】可拖拽任务面板
│   ├── CheckinNode.vue               # 【新增】单个任务节点组件
│   ├── CheckinEdge.vue               # 【新增】连线组件（SVG 箭头）
│   ├── CheckinRoadmap.vue            # 【保留改造】执行阶段的 Roadmap 视图
│   └── CheckinTaskView.vue           # 【保留】单任务执行视图
├── stores/
│   └── checkin.ts                    # 【改造】新增 planEditor 相关状态
└── types/
    └── checkin.ts                    # 【改造】新增 PlanNode, PlanEdge
```

### 4.1 组件层级关系

```
CheckinPlanEditor.vue  （通过 editorStore.openComponentTab 在编辑区打开）
├── CheckinTaskPalette.vue    左侧：可拖拽任务列表
│   └── 每个已安装任务作为可拖拽卡片
│       拖拽开始 → 携带 taskId + taskName + category
│
└── CheckinPlanCanvas.vue     右侧：可视化画布
    ├── <svg> 层：连线渲染
    │   └── CheckinEdge.vue × N  （SVG <path> + 箭头标记）
    │
    └── <div> 层：节点渲染
        └── CheckinNode.vue × N   （绝对定位的彩色卡片）
```

---

## 五、画布编辑器详细设计

### 5.1 CheckinPlanCanvas.vue — 画布核心

```
┌──────────────────────────────────────────────────────────────────┐
│  工具栏: [保存] [撤销] [重做] [自动排列] [缩放: - 100% +]        │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  画布区域（支持拖放 + 滚轮缩放 + 拖拽平移）                        │
│                                                                  │
│     ┌──────────┐  主线(蓝)  ┌──────────┐  主线(蓝)  ┌──────────┐ │
│     │ 每日代码  │═══════════▶│ 阅读学习  │═══════════▶│ 冥想放松  │ │
│     │  🟡 编程  │           │  🟢 学习  │           │  🟢 健康  │ │
│     └─────┬────┘           └──────────┘           └──────────┘ │
│           │                                                      │
│           ├── 支线A(橙)                                          │
│           ▼                                                      │
│     ┌──────────┐  支线A(橙)  ┌──────────┐                        │
│     │ 运动健身  │───────────▶│ 早起打卡  │                        │
│     │  🟡 健康  │           │  🔴 习惯  │                        │
│     └──────────┘           └──────────┘                        │
│           │                                                      │
│           └── 支线B(绿)                                          │
│               ▼                                                  │
│          ┌──────────┐                                            │
│          │ 写日记   │                                            │
│          │  🟢 习惯  │                                            │
│          └──────────┘                                            │
│                                                                  │
│  主线:  ═══ 蓝色加粗   — 必须完成的核心路径                        │
│  支线A: ─── 橙色       — 推荐完成的辅助任务                        │
│  支线B: ··· 绿色虚线   — 完全可选的低优先级任务                    │
│                                                                  │
│  空计划时：显示占位提示"从左侧拖入任务开始编排"                     │
│                                                                  │
├──────────────────────────────────────────────────────────────────┤
│  状态栏: 节点数: 6 | 连线数: 5 (主线2 / 支线A2 / 支线B1) | 已修改  │
└──────────────────────────────────────────────────────────────────┘
```

#### 画布交互功能

| 功能 | 实现方式 | 说明 |
|------|---------|------|
| **拖入节点** | HTML5 Drag & Drop API + `@drop` 事件 | 从左侧面板拖入，在鼠标位置生成新节点 |
| **移动节点** | `@mousedown` 开始拖拽，`@mousemove` 更新坐标 | 节点内按下鼠标拖动 |
| **创建连线（主线）** | 从节点右侧"连接点"**左键**拖出到目标节点 | 蓝色主线 |
| **创建连线（支线A）** | 从节点右侧"连接点"**右键**拖出到目标节点 | 橙色支线A |
| **创建连线（支线B）** | 选中连线 → 右键菜单选择"切换为支线B" | 或按住 `Shift` + 右键拖拽创建 |
| **切换连线类型** | 选中连线 → 右键菜单：主线 / 支线A / 支线B | 三选一轮换 |
| **选中节点** | 单击节点 → 高亮边框 | 用于后续删除/编辑操作 |
| **删除节点** | 选中后按 `Delete` / `Backspace` | 同时删除关联的所有连线 |
| **删除连线** | 选中连线后按 `Delete` / 双击连线 | 高亮后删除 |
| **画布平移** | 空白区域 `@mousedown` + `@mousemove` | 或按住 `Space` + 拖拽 |
| **画布缩放** | `@wheel` 事件 | Ctrl + 滚轮缩放，范围 25% ~ 200% |
| **撤销/重做** | 维护操作历史栈 | Ctrl+Z / Ctrl+Y |

#### 画布状态

```typescript
// CheckinPlanCanvas.vue 内部状态
const canvasState = reactive({
  scale: 1,                     // 缩放比例
  offsetX: 0,                   // 画布平移 X
  offsetY: 0,                   // 画布平移 Y
  nodes: [] as PlanNode[],      // 当前画布节点
  edges: [] as PlanEdge[],      // 当前画布连线
  selectedNodeId: null as string | null,
  selectedEdgeId: null as string | null,
  isDirty: false,               // 是否有未保存修改
})

// 撤销栈
const undoStack: { nodes: PlanNode[]; edges: PlanEdge[] }[] = []
const redoStack: { nodes: PlanNode[]; edges: PlanEdge[] }[] = []
```

### 5.2 CheckinTaskPalette.vue — 任务面板

```
┌─────────────────────────┐
│  任务面板               │
│                         │
│  🔍 [搜索任务...]       │
│                         │
│  ┌───────────────────┐  │
│  │ 🟡 每日代码        │  │  ← 拖拽卡片
│  │   每天编写代码1小时 │  │     draggable="true"
│  └───────────────────┘  │     @dragstart → setData
│  ┌───────────────────┐  │
│  │ 🟢 阅读学习        │  │
│  │   每天阅读技术书籍  │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ 🔴 早起打卡        │  │
│  │   每天早上7点前起床 │  │
│  └───────────────────┘  │
│                         │
└─────────────────────────┘
```

- 数据来源：`checkinStore.installedTasks`（已安装的任务）
- 支持搜索过滤
- 每个卡片携带 `{ taskId, taskName, category }` 数据
- 拖拽时浏览器原生拖拽光标 + 半透明预览

### 5.3 CheckinNode.vue — 任务节点

```
┌─────────────────────┐
│  ●───────┐          │  ← 左侧：输入连接点（sourceHandle）
│  │ 每日代码│          │  ← 节点主体：彩色背景 + 任务名
│  │ 🟡 编程 │          │
│  └───────●──────────│  ← 右侧：输出连接点（targetHandle）
└─────────────────────┘
```

#### 节点视觉规范

- **尺寸**：160px × 72px
- **圆角**：10px
- **颜色方案**（按 category）：

  | 分类 | 背景色 | 边框色 | 连接点色 |
  |------|--------|--------|---------|
  | 编程 | `#fef3c7` (amber-100) | `#f59e0b` (amber-500) | `#d97706` |
  | 学习 | `#dbeafe` (blue-100) | `#3b82f6` (blue-500) | `#2563eb` |
  | 健康 | `#d1fae5` (emerald-100) | `#10b981` (emerald-500) | `#059669` |
  | 习惯 | `#ede9fe` (violet-100) | `#8b5cf6` (violet-500) | `#7c3aed` |
  | 其他 | `#f3f4f6` (gray-100) | `#9ca3af` (gray-400) | `#6b7280` |

- **连接点**：节点左右两侧各一个 10px 圆形手柄，默认半透明，hover 时高亮
- **选中态**：边框加粗 + 外围发光阴影
- **完成态**（执行阶段）：右下角显示绿色 ✓ 标记

### 5.4 CheckinEdge.vue — SVG 连线

```svg
<svg> 覆盖层，位于节点层下方
  <defs>
    <marker id="arrowhead-main">   蓝色箭头定义 </marker>
    <marker id="arrowhead-branch"> 橙色箭头定义 </marker>
    <marker id="arrowhead-side">   绿色箭头定义 </marker>
  </defs>
  <path d="M x1 y1 C ... x2 y2" marker-end="url(#arrowhead-{{ edge.type }})" />
</svg>
```

- 使用 **三次贝塞尔曲线** 绘制，从源节点右侧连接点 → 目标节点左侧连接点
- 视觉区分：

  | 类型 | 线宽 | 颜色 | 线型 | 视觉含义 |
  |------|------|------|------|---------|
  | **主线** (main) | 3px | `#3b82f6` (blue-500) | 实线 | 必须完成的核心路径 |
  | **支线A** (branch) | 2px | `#f97316` (orange-500) | 实线 | 推荐完成的辅助任务 |
  | **支线B** (side) | 2px | `#10b981` (emerald-500) | 虚线 `stroke-dasharray="6,3"` | 完全可选的低优先级任务 |

- 箭头使用 SVG `<marker>` 定义，与线同色
- 选中态：线变粗 + 加亮边框

---

## 六、连线交互详细设计

### 6.1 创建连线流程

连线类型通过**鼠标按键 + 修饰键**区分：

| 操作 | 创建的连线类型 | 临时线预览 |
|------|--------------|-----------|
| **左键拖拽**连接点 | `'main'`（主线） | 蓝色实线 |
| **右键拖拽**连接点 | `'branch'`（支线A） | 橙色实线 |
| **`Shift` + 右键拖拽**连接点 | `'side'`（支线B） | 绿色虚线 |

```
用户操作                          系统响应
────────                          ────────
鼠标移到节点右侧连接点上          → 连接点高亮 + cursor: crosshair
                                  → 浮现提示"左键=主线 | 右键=支线A | Shift+右键=支线B"
按下鼠标左键                      → 记录 edgeType = 'main'
  或 按下鼠标右键                 → 记录 edgeType = 'branch'
  或 按下 Shift + 鼠标右键        → 记录 edgeType = 'side'
                                  → 创建临时 SVG <line> 跟随鼠标
                                  → 根据 edgeType 显示对应颜色/线型预览
拖动鼠标（mousemove）             → 更新临时线终点为鼠标坐标
鼠标移到目标节点上                → 目标节点高亮（表示可连接）
释放鼠标（mouseup）               → 目标节点有效？
                                  ├─ 是 → 创建 PlanEdge{ type }
                                  └─ 否 → 取消，移除临时线
```

### 6.2 连线规则

- 同一对节点之间只允许一条连线（不可重复连接）
- 不允许节点连接自身（`sourceNodeId !== targetNodeId`）
- 允许一个节点连接多个目标（一对多）
- 允许多个节点连接同一目标（多对一）
- 允许形成非循环图（建议但不强制检测循环）
- 允许孤立节点（无任何连线的节点）
- 允许同一节点同时有主线和支线出边（如：日常学习 → 主线去阅读 + 支线去运动）

### 6.3 三类连线语义与执行行为

#### 设计意图（按优先级分层）

| 类型 | 含义 | 视觉特征 | 执行行为 |
|------|------|----------|---------|
| **主线** (main) | 打卡计划的**核心路径**，必须完成 | 3px 蓝色实线 | 按序锁定：前一个未完成则后续主线节点不可开始 |
| **支线A** (branch) | **推荐完成**的辅助任务 | 2px 橙色实线 | 可随时开始，不受主线进度限制，建议每天完成 |
| **支线B** (side) | **完全可选**的低优先级任务 | 2px 绿色虚线 | 空闲时间做，不做不影响打卡完成度，想做就做 |

#### 典型使用场景

```
                ┌─────────┐
                │ 起床打卡  │
                └────┬────┘
                     │ 主线（蓝）
                     ▼
                ┌─────────┐
                │ 背单词   │──────────┐
                └────┬────┘           │ 支线A（橙，推荐）
                     │ 主线           ▼
                     ▼           ┌─────────┐
                ┌─────────┐     │ 喝咖啡   │
                │ 刷算法题 │     └─────────┘
                └────┬────┘
                     │ 主线
                     ├───┐
                     │   ▼
                     │ ┌─────────┐
                     │ │ 写日记   │  ← 支线B（绿虚线，可选）
                     │ └─────────┘
                     ▼
                ┌─────────┐
                │ 提交日记  │
                └─────────┘
```

在这个例子中：
- **主线**：起床打卡 → 背单词 → 刷算法题 → 提交日记（必须按顺序完成）
- **支线A**：喝咖啡（推荐完成，每天喝一杯，可以随时打卡）
- **支线B**：写日记（可选，有心情就写，不写不影响当日打卡完成）

#### 连线类型切换

用户可以在创建连线后随时更改其类型：
- 选中连线 → 右键菜单：`主线 → 支线A → 支线B → 主线` 三轮换
- 或在底部属性面板用下拉框选择

### 6.4 执行阶段解锁规则

执行打卡时：
- 主线节点：只有当**所有入边中所有主线路径上的节点都完成**时，才解锁
- 支线A / 支线B：始终处于解锁状态，可随时打卡

---

## 七、CheckinPanel.vue 改造

### 7.1 新建计划弹窗简化

**改造前**：
```
┌──────────────────────┐
│  新建打卡计划         │
│                      │
│  计划名称: [______]  │
│  计划描述: [______]  │
│                      │
│  选择任务:           │
│  ☑ 每日代码          │
│  ☐ 阅读学习          │
│  ☐ 运动健身          │
│                      │
│  [取消]  [确定]      │
└──────────────────────┘
```

**改造后**：
```
┌──────────────────────┐
│  新建打卡计划         │
│                      │
│  计划名称: [______]  │
│                      │
│  创建后可拖拽任务     │
│  编排打卡流程         │
│                      │
│  [取消]  [确定]      │
└──────────────────────┘
```

### 7.2 计划列表项改造

点击计划名称 → 不再打开 `CheckinRoadmap`，而是打开 `CheckinPlanEditor`：

```typescript
// CheckinPanel.vue 中的点击处理
function openPlanEditor(plan: CheckinPlan) {
  editorStore.openComponentTab(
    `checkin:editor:${plan.id}`,
    plan.name,
    CheckinPlanEditor,
    { planId: plan.id }
  )
}
```

同时保留 Roadmap 入口（小 Play 按钮），用于进入执行模式。

---

## 八、Store 改造

### 8.1 checkinStore 新增/修改方法

```typescript
// stores/checkin.ts

// ===== 修改：createPlan 不再需要 taskIds =====
function createPlan(name: string): CheckinPlan {
  const newPlan: CheckinPlan = {
    id: `plan-${Date.now()}`,
    name,
    createdAt: new Date().toISOString().split('T')[0],
    nodes: [],          // 空 nodes，稍后在画布中添加
    edges: [],          // 空 edges
  }
  myPlans.value.push(newPlan)
  saveMyPlans()
  return newPlan
}

// ===== 新增：更新计划结构（保存画布内容）=====
function updatePlanStructure(
  planId: string,
  nodes: PlanNode[],
  edges: PlanEdge[]
) {
  const plan = myPlans.value.find(p => p.id === planId)
  if (plan) {
    plan.nodes = nodes
    plan.edges = edges
    saveMyPlans()
  }
}

// ===== 新增：获取计划的第一个节点（Roadmap 入口）=====
function getPlanEntryNode(planId: string): PlanNode | null {
  const plan = myPlans.value.find(p => p.id === planId)
  if (!plan || plan.nodes.length === 0) return null
  
  // 找到没有入边的节点（源节点）作为入口
  const nodesWithIncoming = new Set(plan.edges.map(e => e.targetNodeId))
  return plan.nodes.find(n => !nodesWithIncoming.has(n.id)) || plan.nodes[0]
}

// ===== 修改：completeTask 适配新结构 =====
function completeTask(planId: string, nodeId: string) {
  const plan = myPlans.value.find(p => p.id === planId)
  if (plan) {
    const node = plan.nodes.find(n => n.id === nodeId)
    if (node) {
      node.completed = true
      node.completedAt = new Date().toISOString()
      saveMyPlans()
    }
  }
}

// ===== 新增：获取某个节点的后续节点 =====
function getNextNodes(planId: string, nodeId: string, edgeType?: 'main' | 'branch' | 'side'): PlanNode[] {
  const plan = myPlans.value.find(p => p.id === planId)
  if (!plan) return []
  const nextNodeIds = plan.edges
    .filter(e => e.sourceNodeId === nodeId)
    .filter(e => !edgeType || e.type === edgeType)
    .map(e => e.targetNodeId)
  return plan.nodes.filter(n => nextNodeIds.includes(n.id))
}

// ===== 新增：获取主线路径（从入口节点开始的所有主线下游节点）=====
function getMainPathNodes(planId: string): PlanNode[] {
  const plan = myPlans.value.find(p => p.id === planId)
  if (!plan) return []
  const entry = getPlanEntryNode(planId)
  if (!entry) return []
  
  const result: PlanNode[] = []
  const visited = new Set<string>()
  const queue = [entry.id]
  
  while (queue.length > 0) {
    const nodeId = queue.shift()!
    if (visited.has(nodeId)) continue
    visited.add(nodeId)
    const node = plan.nodes.find(n => n.id === nodeId)
    if (node) result.push(node)
    // 只沿主线扩展
    plan.edges
      .filter(e => e.sourceNodeId === nodeId && e.type === 'main')
      .forEach(e => queue.push(e.targetNodeId))
  }
  return result
}
```

### 8.2 返回值更新

```typescript
return {
  // ... 原有属性
  createPlan,           // 签名改为 (name: string)
  updatePlanStructure,  // 新增：保存画布内容
  getPlanEntryNode,     // 新增：获取入口节点
  getNextNodes,         // 新增：获取后续节点（支持按 edgeType 过滤）
  getMainPathNodes,     // 新增：获取主线路径所有节点
  // completeTask 参数改为 nodeId
}
```

---

## 九、旧数据兼容

由于 localStorage 中可能存在旧格式的 `CheckinPlan`（含 `tasks: PlanTask[]`），需要在加载时做兼容处理：

```typescript
// stores/checkin.ts — loadData() 中添加

function migratePlan(plan: any): CheckinPlan {
  // 已经是新格式
  if (plan.nodes && plan.edges) return plan as CheckinPlan
  
  // 旧格式迁移：PlanTask[] → PlanNode[] + PlanEdge[]
  if (plan.tasks && Array.isArray(plan.tasks)) {
    const nodes: PlanNode[] = plan.tasks.map((t: PlanTask, i: number) => ({
      id: `node-${Date.now()}-${i}`,
      taskId: t.taskId,
      taskName: t.taskName,
      category: '其他',
      x: 100 + (i % 3) * 200,
      y: 100 + Math.floor(i / 3) * 150,
      completed: t.completed || false,
      completedAt: t.completedAt,
    }))
    
    const edges: PlanEdge[] = []
    for (let i = 0; i < nodes.length - 1; i++) {
      edges.push({
        id: `edge-${Date.now()}-${i}`,
        sourceNodeId: nodes[i].id,
        targetNodeId: nodes[i + 1].id,
        type: 'main',  // 旧数据迁移默认设为主线
      })
    }
    
    return { ...plan, nodes, edges }
  }
  
  // 完全空计划
  return { ...plan, nodes: [], edges: [] }
}

function loadData() {
  const savedPlans = localStorage.getItem('checkin_myPlans')
  if (savedPlans) {
    myPlans.value = JSON.parse(savedPlans).map(migratePlan)
  }
  // ...
}
```

---

## 十、实现不依赖外部库

当前项目依赖中无现成的流程图库（如 vue-flow），考虑到：
- 打卡计划的节点通常在 3~10 个规模，不复杂
- 避免引入大型依赖增加打包体积

**采用纯 Vue 3 + SVG 自绘方案**：

| 功能 | 实现方式 |
|------|---------|
| 节点拖拽 | HTML5 Drag & Drop + `mousedown/mousemove/mouseup` |
| 连线绘制 | SVG `<path>` 贝塞尔曲线 |
| 连接点交互 | 圆形 `<div>` + 事件监听 |
| 画布缩放/平移 | CSS `transform: scale() translate()` |
| 撤销/重做 | 数组栈存储快照 |
| 键盘快捷键 | `@keydown` 全局监听 |

---

## 十一、文件变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/renderer/types/checkin.ts` | 修改 | 新增 `PlanNode`, `PlanEdge`，修改 `CheckinPlan` |
| `src/renderer/stores/checkin.ts` | 修改 | 改造 `createPlan`，新增 `updatePlanStructure` 等 |
| `src/renderer/plugins/builtin/components/CheckinPanel.vue` | 修改 | 简化新建弹窗，计划点击改为打开编辑器 |
| `src/renderer/plugins/builtin/components/CheckinPlanEditor.vue` | **新增** | 编辑器主容器（左右分栏布局） |
| `src/renderer/plugins/builtin/components/CheckinPlanCanvas.vue` | **新增** | SVG/Div 画布核心组件 |
| `src/renderer/plugins/builtin/components/CheckinTaskPalette.vue` | **新增** | 可拖拽任务面板 |
| `src/renderer/plugins/builtin/components/CheckinNode.vue` | **新增** | 彩色任务节点 |
| `src/renderer/plugins/builtin/components/CheckinEdge.vue` | **新增** | SVG 连线箭头 |
| `src/renderer/plugins/builtin/components/CheckinRoadmap.vue` | 修改 | 适配新数据结构 |

---

## 十二、实现优先级与阶段划分

### 第一阶段：基础画布（MVP）
1. `CheckinPlanEditor.vue` — 左右分栏骨架
2. `CheckinTaskPalette.vue` — 任务列表 + 基础拖拽
3. `CheckinPlanCanvas.vue` — 空白画布 + 接受拖放生成节点
4. `CheckinNode.vue` — 彩色节点 + 移动
5. Store 改造 + 数据兼容

### 第二阶段：连线与交互
6. `CheckinEdge.vue` — SVG 连线渲染
7. 创建连线交互（连接点拖拽）
8. 删除节点/连线
9. 画布平移 + 缩放

### 第三阶段：体验增强
10. 撤销/重做
11. 自动排列布局
12. 键盘快捷键
13. Roadmap 执行视图适配

---

## 十三、与现有 Roadmap 执行视图的关系

```
                      ┌─────────────────────┐
                      │   CheckinPanel       │
                      │   （工作区）          │
                      │                     │
                      │  [新建] 按钮          │
                      │    → 创建空计划       │
                      │                     │
                      │  点击计划名称         │
                      │    → 打开编辑器 Tab   │
                      │                     │
                      │  点击 Play 按钮       │
                      │    → 打开 Roadmap Tab │
                      └──────┬──────┬───────┘
                             │      │
              ┌──────────────┘      └──────────────┐
              ▼                                    ▼
┌──────────────────────────┐        ┌──────────────────────────┐
│  CheckinPlanEditor       │        │  CheckinRoadmap          │
│  （编辑区 Tab）           │        │  （编辑区 Tab）           │
│                          │        │                          │
│  编辑模式（画布编辑器）：       │        │  执行模式（Roadmap）：           │
│  - 拖入任务节点                  │        │  - 展示节点图（只读）             │
│  - 左键连线 = 蓝色主线           │        │  - 蓝色主线：按序锁定执行         │
│  - 右键连线 = 橙色支线A           │        │  - 橙色支线A：可随时打卡           │
│  - Shift+右键 = 绿色支线B(虚线)  │        │  - 绿色支线B(虚线)：随时可做      │
│  - 右键菜单三选一轮换            │        │  - 当前主线节点高亮               │
│  - 删除/移动节点                  │        │  - 完成主线节点 → 下一节点解锁    │
│  - [保存] 按钮                   │        │  - 支线A/支线B节点可随时开始      │
└──────────────────────────┘        └──────────────────────────┘
```

两个视图共享同一份 `CheckinPlan` 数据，编辑模式修改结构（nodes/edges），执行模式修改状态（completed）。

---

## 十四、总结

本方案将打卡计划的创建从"填表选任务"升级为"可视化拖拽编排"，核心变化：

1. **创建更轻量** — 只需一个名称即可创建空计划
2. **编排更直观** — 彩色节点 + 三类 SVG 箭头（蓝色主线 / 橙色支线A / 绿色虚线支线B）
3. **关系更灵活** — 从固定的线性顺序升级为带优先级分层的有向图
4. **执行分层** — 主线按序锁定，支线A推荐完成，支线B完全可选，兼顾纪律与弹性
5. **完全本地化** — 纯前端实现，零后端依赖，数据存 localStorage
6. **零新增依赖** — 基于现有 Vue 3 + SVG + Tailwind 技术栈