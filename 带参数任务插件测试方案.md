# 带参数任务插件测试方案

## 一、测试目标

验证插件系统能否完整支持带参数的任务插件流程：
1. **插件创建**：开发者创建一个带参数的打卡任务插件
2. **插件上传**：将插件上传到后端服务
3. **插件下载**：前端从后端下载插件
4. **插件安装**：前端加载并挂载插件
5. **参数配置**：用户在编辑器中配置任务参数
6. **插件使用**：根据不同参数展示不同内容
7. **插件卸载**：前端卸载插件

## 二、测试环境

### 2.1 前端环境
- 框架：Vue 3 + TypeScript
- 插件加载器：`src/renderer/plugins/PluginLoader.ts`
- 测试用户ID：`test-user-001`

### 2.2 后端环境
- 框架：Spring Boot 3.2.x
- 数据库：PostgreSQL 16.x
- API 地址：`http://localhost:8080/api`

## 三、测试插件定义

### 3.1 插件元数据

| 字段 | 值 |
|------|------|
| ID | `test-quiz-task` |
| 名称 | `编程刷题测试插件` |
| 版本 | `1.0.0` |
| 作者 | `Test Author` |
| 分类 | `编程` |
| 描述 | `一个带参数的编程刷题测试插件，支持根据题号展示不同题目` |

### 3.2 插件参数定义

| 参数名 | 参数键 | 类型 | 默认值 | 说明 |
|--------|--------|------|--------|------|
| 题目编号 | questionId | number | 1 | 题目编号 (1-3) |
| 显示提示 | showHints | boolean | true | 是否显示题目提示 |
| 难度筛选 | difficulty | select | all | 难度筛选 (all/easy/medium/hard) |

### 3.3 插件代码（包含预置题目）

```javascript
// test-quiz-task.js

// 预置题目数据直接写在组件内部
const questions = [
  {
    id: 1,
    title: '两数之和',
    difficulty: 'easy',
    description: '给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出和为目标值 target 的那两个整数，并返回它们的数组下标。',
    examples: ['输入：nums = [2,7,11,15], target = 9', '输出：[0,1]'],
    hints: ['可以使用哈希表来优化查找效率'],
    timeLimit: 15
  },
  {
    id: 2,
    title: '有效的括号',
    difficulty: 'medium',
    description: '给定一个只包括括号的字符串 s，判断字符串是否有效。',
    examples: ['输入：s = "()"', '输出：true'],
    hints: ['使用栈来解决这个问题'],
    timeLimit: 20
  },
  {
    id: 3,
    title: '合并K个升序链表',
    difficulty: 'hard',
    description: '给你一个链表数组，每个链表都已经按升序排列，请你将所有链表合并到一个升序链表中。',
    examples: ['输入：lists = [[1,4,5],[1,3,4],[2,6]]', '输出：[1,1,2,3,4,4,5,6]'],
    hints: ['可以使用优先队列来优化'],
    timeLimit: 30
  }
]

const template = `
  <div class="quiz-task-plugin">
    <div class="quiz-header">
      <h3>{{ currentQuestion.title }}</h3>
      <span :class="difficultyClass">{{ difficultyText }}</span>
    </div>
    
    <div class="quiz-timer">
      <span>⏱️ {{ formatTime(elapsedTime) }}</span>
      <span class="time-limit">建议时间: {{ currentQuestion.timeLimit }}分钟</span>
    </div>
    
    <div class="quiz-description">
      <p>{{ currentQuestion.description }}</p>
    </div>
    
    <div class="quiz-examples">
      <h4>示例:</h4>
      <div v-for="(example, index) in currentQuestion.examples" :key="index" class="example-item">
        {{ example }}
      </div>
    </div>
    
    <div v-if="showHintsPanel && currentQuestion.hints.length" class="quiz-hints">
      <h4 @click="toggleHints" class="hints-toggle">
        💡 提示 (点击展开)
      </h4>
      <div v-if="hintsExpanded" class="hints-content">
        <p v-for="(hint, index) in currentQuestion.hints" :key="index">{{ hint }}</p>
      </div>
    </div>
    
    <div class="quiz-actions">
      <button @click="startCoding" class="btn-primary">开始编码</button>
      <button @click="completeTask" class="btn-success">完成打卡</button>
    </div>
    
    <div v-if="completed" class="quiz-complete">
      ✅ 任务完成！用时 {{ formatTime(elapsedTime) }}
    </div>
  </div>
`

const style = `
  <style>
    .quiz-task-plugin {
      padding: 24px;
      background: #1e1e1e;
      border-radius: 12px;
      max-width: 800px;
      margin: 0 auto;
    }
    .quiz-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 16px;
    }
    .quiz-header h3 {
      color: #fff;
      font-size: 24px;
      margin: 0;
    }
    .difficulty-easy { color: #4caf50; }
    .difficulty-medium { color: #ff9800; }
    .difficulty-hard { color: #f44336; }
    .quiz-timer {
      display: flex;
      justify-content: space-between;
      padding: 12px;
      background: #2d2d2d;
      border-radius: 8px;
      margin-bottom: 16px;
      color: #fff;
    }
    .time-limit {
      color: #888;
      font-size: 14px;
    }
    .quiz-description {
      padding: 16px;
      background: #2d2d2d;
      border-radius: 8px;
      margin-bottom: 16px;
      color: #ccc;
      line-height: 1.6;
    }
    .quiz-examples {
      margin-bottom: 16px;
    }
    .quiz-examples h4 {
      color: #888;
      margin-bottom: 8px;
    }
    .example-item {
      padding: 8px 12px;
      background: #2d2d2d;
      border-radius: 4px;
      margin-bottom: 4px;
      font-family: monospace;
      color: #fff;
      font-size: 14px;
    }
    .quiz-hints {
      margin-bottom: 16px;
    }
    .hints-toggle {
      color: #007acc;
      cursor: pointer;
      margin-bottom: 8px;
    }
    .hints-content {
      padding: 12px;
      background: #2d2d2d;
      border-radius: 8px;
      color: #888;
    }
    .hints-content p {
      margin: 4px 0;
    }
    .quiz-actions {
      display: flex;
      gap: 12px;
      margin-top: 24px;
    }
    .btn-primary, .btn-success {
      flex: 1;
      padding: 12px 24px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      font-weight: 600;
    }
    .btn-primary {
      background: #007acc;
      color: #fff;
    }
    .btn-primary:hover {
      background: #005a9e;
    }
    .btn-success {
      background: #4caf50;
      color: #fff;
    }
    .btn-success:hover {
      background: #388e3c;
    }
    .quiz-complete {
      margin-top: 16px;
      padding: 16px;
      background: #1b5e20;
      border-radius: 8px;
      color: #fff;
      text-align: center;
      font-size: 18px;
    }
  </style>
`

export default {
  name: 'TestQuizTask',
  props: {
    // 从插件系统传入的参数
    params: {
      type: Object,
      default: () => ({})
    }
  },
  data() {
    return {
      // 预置题目数据在组件内部
      questions: questions,
      elapsedTime: 0,
      completed: false,
      hintsExpanded: false,
      timer: null
    }
  },
  computed: {
    currentQuestion() {
      const questionId = this.params.questionId || 1
      return this.questions.find(q => q.id === questionId) || this.questions[0]
    },
    showHintsPanel() {
      return this.params.showHints !== false
    },
    difficultyText() {
      const map = { easy: '简单', medium: '中等', hard: '困难' }
      return map[this.currentQuestion.difficulty] || '未知'
    },
    difficultyClass() {
      return `difficulty-${this.currentQuestion.difficulty}`
    }
  },
  mounted() {
    console.log('TestQuizTask plugin mounted with params:', this.params)
    this.startTimer()
  },
  beforeUnmount() {
    this.stopTimer()
  },
  methods: {
    startTimer() {
      this.timer = setInterval(() => {
        this.elapsedTime++
      }, 1000)
    },
    stopTimer() {
      if (this.timer) {
        clearInterval(this.timer)
        this.timer = null
      }
    },
    formatTime(seconds) {
      const mins = Math.floor(seconds / 60)
      const secs = seconds % 60
      return `${mins.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`
    },
    toggleHints() {
      this.hintsExpanded = !this.hintsExpanded
    },
    startCoding() {
      console.log('Starting coding for question:', this.currentQuestion.title)
      alert(`请在本地 IDE 中完成题目: ${this.currentQuestion.title}`)
    },
    completeTask() {
      this.stopTimer()
      this.completed = true
      console.log('Task completed! Time:', this.formatTime(this.elapsedTime))
      
      // 通知插件系统任务完成
      if (this.$emit) {
        this.$emit('complete', {
          questionId: this.currentQuestion.id,
          time: this.elapsedTime
        })
      }
    }
  },
  template,
  style
}
```

### 3.4 插件清单 (Manifest)

```json
{
  "id": "test-quiz-task",
  "name": "编程刷题测试插件",
  "version": "1.0.0",
  "description": "一个带参数的编程刷题测试插件，支持根据题号展示不同题目",
  "author": "Test Author",
  "category": "编程",
  "icon": "💻",
  "dependencies": [],
  "entry": "index.js",
  "params": [
    {
      "name": "题目编号",
      "key": "questionId",
      "type": "number",
      "default": 1,
      "min": 1,
      "max": 3,
      "placeholder": "请输入题目编号 (1-3)"
    },
    {
      "name": "显示提示",
      "key": "showHints",
      "type": "boolean",
      "default": true
    },
    {
      "name": "难度筛选",
      "key": "difficulty",
      "type": "select",
      "default": "all",
      "options": [
        { "label": "全部", "value": "all" },
        { "label": "简单", "value": "easy" },
        { "label": "中等", "value": "medium" },
        { "label": "困难", "value": "hard" }
      ]
    }
  ],
  "createdAt": "2024-01-01T00:00:00Z",
  "updatedAt": "2024-01-01T00:00:00Z"
}
```

> **注意**：预置题目数据直接写在组件代码中，不需要在 Manifest 中单独定义 data 字段。这样插件更加自包含，所有逻辑和数据都在组件内部。

## 四、参数传递机制

### 4.1 数据流

```
1. 任务模板定义参数配置 (CheckinTask.params)
    ↓
2. 用户拖拽任务到画布创建节点 (PlanNode)
    ↓
3. 用户选中节点，右键或点击工具栏配置参数
    ↓
4. 参数值保存到 PlanNode.params
    ↓
5. 执行任务时，params 通过 props 传递给任务视图组件
    ↓
6. 任务视图组件根据 params 展示不同内容
```

### 4.2 核心类型定义

```typescript
// src/renderer/types/checkin.ts

// 任务参数定义
export interface TaskParam {
  name: string              // 参数名称（用于显示）
  key: string               // 参数键（用于存储）
  type: 'number' | 'string' | 'select' | 'boolean'  // 参数类型
  default: unknown          // 默认值
  options?: { label: string; value: unknown }[]  // select类型的选项
  min?: number              // number类型的最小值
  max?: number              // number类型的最大值
  placeholder?: string      // 输入提示
}

// 任务模板
export interface CheckinTask {
  id: string
  name: string
  // ... 其他字段
  params?: TaskParam[]       // 任务参数配置
}

// 画布节点
export interface PlanNode {
  id: string
  taskId: string
  taskName: string
  // ... 其他字段
  params?: Record<string, unknown>  // 任务参数值
}
```

### 4.3 参数配置组件

项目已实现 `TaskParamForm.vue` 组件，支持以下参数类型：

| 类型 | 渲染组件 | 说明 |
|------|----------|------|
| number | `<input type="number">` | 支持 min/max 限制 |
| string | `<input type="text">` | 支持 placeholder |
| select | `<select>` | 需提供 options |
| boolean | `<input type="checkbox">` | 开关切换 |

### 4.4 参数配置入口

在 `CheckinPlanCanvas.vue` 中提供两种配置入口：

1. **工具栏按钮**：选中节点后，如果任务有参数配置，工具栏会显示齿轮图标
2. **右键菜单**：选中节点后右键，显示"配置参数"选项

### 4.5 参数传递

在 `CheckinRoadmap.vue` 的 `openTask` 函数中：

```typescript
function openTask(node: PlanNode) {
  editorStore.openComponentTab(
    `checkin:task:${node.id}`,
    node.taskName,
    CheckinTaskView,
    { 
      planId: plan.value.id, 
      taskId: node.id, 
      taskName: node.taskName,
      params: node.params || {}  // 传递节点参数
    }
  )
}
```

### 4.6 参数接收

在 `CheckinTaskView.vue` 中：

```typescript
const props = defineProps<{
  planId: string
  taskId: string
  taskName: string
  params?: Record<string, unknown>  // 接收参数
}>()

// 使用参数
const questionId = computed(() => props.params?.questionId || 1)
const showHints = computed(() => props.params?.showHints !== false)
```

## 五、测试步骤

### 5.1 步骤一：启动后端服务

```bash
# 进入后端项目目录
cd backend

# 启动 Spring Boot 应用
mvn spring-boot:run
```

验证：访问 `http://localhost:8080/api/plugins` 应返回空数组或已有插件列表。

### 5.2 步骤二：上传测试插件

使用 curl 或 Postman 上传插件：

```bash
curl -X POST http://localhost:8080/api/plugins \
  -H "Content-Type: application/json" \
  -d @- << 'EOF'
{
  "id": "test-quiz-task",
  "name": "编程刷题测试插件",
  "version": "1.0.0",
  "description": "一个带参数的编程刷题测试插件",
  "author": "Test Author",
  "category": "编程",
  "icon": "💻",
  "params": [
    {
      "name": "题目编号",
      "key": "questionId",
      "type": "number",
      "default": 1,
      "min": 1,
      "max": 3
    },
    {
      "name": "显示提示",
      "key": "showHints",
      "type": "boolean",
      "default": true
    },
    {
      "name": "难度筛选",
      "key": "difficulty",
      "type": "select",
      "default": "all",
      "options": [
        { "label": "全部", "value": "all" },
        { "label": "简单", "value": "easy" },
        { "label": "中等", "value": "medium" },
        { "label": "困难", "value": "hard" }
      ]
    }
  ],
  "code": "const questions = [...]; export default { ... }"
}
EOF
```

> **注意**：`code` 字段包含完整的插件代码（包括预置题目数据），这里简化表示。实际使用时应将完整的组件代码字符串传入。

验证：检查数据库 `plugins` 表是否新增了 `test-quiz-task` 记录。

### 5.3 步骤三：前端下载并安装插件

创建参数配置组件 `TaskParamConfig.vue`：

```vue
<script setup lang="ts">
import { reactive, watch } from 'vue'

interface TaskParam {
  name: string
  key: string
  type: 'number' | 'string' | 'select' | 'boolean'
  default: unknown
  options?: { label: string; value: unknown }[]
  min?: number
  max?: number
  placeholder?: string
}

const props = defineProps<{
  params: TaskParam[]
  modelValue?: Record<string, unknown>
}>()

const emit = defineEmits<{
  (e: 'update:modelValue', value: Record<string, unknown>): void
}>()

const localParams = reactive<Record<string, unknown>>({})

// 初始化默认值
props.params.forEach(param => {
  localParams[param.key] = props.modelValue?.[param.key] ?? param.default
})

// 监听外部变化
watch(() => props.modelValue, (newValue) => {
  if (newValue) {
    Object.assign(localParams, newValue)
  }
}, { deep: true })

function updateParam(key: string, value: unknown) {
  localParams[key] = value
  emit('update:modelValue', { ...localParams })
}
</script>

<template>
  <div class="param-config">
    <h3>任务参数配置</h3>
    
    <div v-for="param in params" :key="param.key" class="param-item">
      <label>{{ param.name }}</label>
      
      <!-- 数字输入 -->
      <input
        v-if="param.type === 'number'"
        type="number"
        :value="localParams[param.key]"
        :min="param.min"
        :max="param.max"
        :placeholder="param.placeholder"
        @input="updateParam(param.key, Number(($event.target as HTMLInputElement).value))"
      />
      
      <!-- 布尔开关 -->
      <label v-else-if="param.type === 'boolean'" class="switch">
        <input
          type="checkbox"
          :checked="localParams[param.key] === true"
          @change="updateParam(param.key, ($event.target as HTMLInputElement).checked)"
        />
        <span class="slider"></span>
      </label>
      
      <!-- 下拉选择 -->
      <select
        v-else-if="param.type === 'select'"
        :value="localParams[param.key]"
        @change="updateParam(param.key, ($event.target as HTMLSelectElement).value)"
      >
        <option v-for="opt in param.options" :key="opt.value" :value="opt.value">
          {{ opt.label }}
        </option>
      </select>
    </div>
  </div>
</template>

<style scoped>
.param-config {
  padding: 16px;
  background: #1e1e1e;
  border-radius: 8px;
}

.param-config h3 {
  color: #fff;
  margin-bottom: 16px;
}

.param-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12px;
}

.param-item label {
  color: #ccc;
}

.param-item input[type="number"],
.param-item select {
  padding: 8px 12px;
  background: #2d2d2d;
  border: 1px solid #444;
  border-radius: 4px;
  color: #fff;
  min-width: 150px;
}

.switch {
  position: relative;
  width: 50px;
  height: 26px;
}

.switch input {
  opacity: 0;
  width: 0;
  height: 0;
}

.slider {
  position: absolute;
  cursor: pointer;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: #444;
  transition: 0.3s;
  border-radius: 26px;
}

.slider:before {
  position: absolute;
  content: "";
  height: 20px;
  width: 20px;
  left: 3px;
  bottom: 3px;
  background-color: white;
  transition: 0.3s;
  border-radius: 50%;
}

input:checked + .slider {
  background-color: #007acc;
}

input:checked + .slider:before {
  transform: translateX(24px);
}
</style>
```

### 5.4 步骤四：创建测试组件

创建测试组件 `ParamPluginTest.vue`：

```vue
<script setup lang="ts">
import { ref, reactive, onMounted, onUnmounted } from 'vue'
import { pluginLoader } from '@/plugins/PluginLoader'
import TaskParamConfig from './TaskParamConfig.vue'

const pluginInstance = ref(null)
const isLoading = ref(false)
const error = ref('')
const installedPlugins = ref<string[]>([])

// 参数配置
const currentParams = reactive<Record<string, unknown>>({
  questionId: 1,
  showHints: true,
  difficulty: 'all'
})

// 插件参数定义（从插件 Manifest 中获取）
const pluginParams = [
  {
    name: '题目编号',
    key: 'questionId',
    type: 'number' as const,
    default: 1,
    min: 1,
    max: 3,
    placeholder: '请输入题目编号 (1-3)'
  },
  {
    name: '显示提示',
    key: 'showHints',
    type: 'boolean' as const,
    default: true
  },
  {
    name: '难度筛选',
    key: 'difficulty',
    type: 'select' as const,
    default: 'all',
    options: [
      { label: '全部', value: 'all' },
      { label: '简单', value: 'easy' },
      { label: '中等', value: 'medium' },
      { label: '困难', value: 'hard' }
    ]
  }
]

async function loadPlugin() {
  isLoading.value = true
  error.value = ''
  
  try {
    const instance = await pluginLoader.install('test-quiz-task')
    pluginInstance.value = instance
    installedPlugins.value = pluginLoader.getAllPlugins().map(p => p.manifest.id)
    console.log('Plugin installed successfully:', instance.manifest.name)
  } catch (err) {
    error.value = (err as Error).message
    console.error('Failed to install plugin:', err)
  } finally {
    isLoading.value = false
  }
}

async function unloadPlugin() {
  try {
    await pluginLoader.uninstall('test-quiz-task')
    pluginInstance.value = null
    installedPlugins.value = pluginLoader.getAllPlugins().map(p => p.manifest.id)
    console.log('Plugin uninstalled successfully')
  } catch (err) {
    error.value = (err as Error).message
    console.error('Failed to uninstall plugin:', err)
  }
}

function handleParamsUpdate(newParams: Record<string, unknown>) {
  Object.assign(currentParams, newParams)
  console.log('Params updated:', currentParams)
}

function handleTaskComplete(data: { questionId: number; time: number }) {
  console.log('Task completed:', data)
  alert(`题目 ${data.questionId} 完成！用时 ${Math.floor(data.time / 60)}分${data.time % 60}秒`)
}

function refreshPlugins() {
  installedPlugins.value = pluginLoader.getAllPlugins().map(p => p.manifest.id)
}

onMounted(() => {
  refreshPlugins()
})

onUnmounted(() => {
  if (pluginInstance.value) {
    pluginLoader.uninstall('test-quiz-task').catch(console.error)
  }
})
</script>

<template>
  <div class="plugin-test-container">
    <h2>带参数任务插件测试</h2>
    
    <div class="test-controls">
      <button @click="loadPlugin" :disabled="isLoading || installedPlugins.includes('test-quiz-task')">
        {{ isLoading ? '安装中...' : (installedPlugins.includes('test-quiz-task') ? '已安装' : '安装插件') }}
      </button>
      <button 
        @click="unloadPlugin" 
        :disabled="!installedPlugins.includes('test-quiz-task')"
      >
        卸载插件
      </button>
    </div>
    
    <div v-if="error" class="error-message">{{ error }}</div>
    
    <div class="installed-list">
      <h3>已安装插件:</h3>
      <ul>
        <li v-for="pluginId in installedPlugins" :key="pluginId">{{ pluginId }}</li>
        <li v-if="installedPlugins.length === 0">暂无已安装插件</li>
      </ul>
    </div>
    
    <!-- 参数配置区域 -->
    <div v-if="installedPlugins.includes('test-quiz-task')" class="param-section">
      <h3>参数配置</h3>
      <TaskParamConfig 
        :params="pluginParams"
        v-model="currentParams"
        @update:modelValue="handleParamsUpdate"
      />
    </div>
    
    <!-- 插件展示区域 -->
    <div class="plugin-container">
      <h3>插件展示区:</h3>
      <div v-if="isLoading" class="loading">加载插件中...</div>
      <component 
        v-else-if="pluginInstance" 
        :is="pluginInstance.component"
        :params="currentParams"
        @complete="handleTaskComplete"
      />
      <div v-else class="empty">点击"安装插件"加载测试插件</div>
    </div>
    
    <!-- 当前参数显示 -->
    <div class="current-params">
      <h3>当前参数值:</h3>
      <pre>{{ JSON.stringify(currentParams, null, 2) }}</pre>
    </div>
  </div>
</template>

<style scoped>
.plugin-test-container {
  padding: 20px;
  max-width: 1000px;
  margin: 0 auto;
}

.test-controls {
  display: flex;
  gap: 10px;
  margin-bottom: 20px;
}

.test-controls button {
  padding: 8px 16px;
  cursor: pointer;
  background: #007acc;
  color: white;
  border: none;
  border-radius: 4px;
}

.test-controls button:disabled {
  background: #666;
  cursor: not-allowed;
}

.error-message {
  color: #f44336;
  margin-bottom: 10px;
}

.installed-list {
  margin-bottom: 20px;
}

.installed-list ul {
  list-style-type: none;
  padding: 0;
}

.installed-list li {
  padding: 4px 0;
  color: #888;
}

.param-section {
  margin-bottom: 20px;
  padding: 16px;
  background: #252526;
  border-radius: 8px;
}

.param-section h3 {
  color: #fff;
  margin-bottom: 12px;
}

.plugin-container {
  border: 1px solid #333;
  border-radius: 8px;
  padding: 20px;
  min-height: 400px;
  margin-bottom: 20px;
}

.plugin-container h3 {
  color: #fff;
  margin-bottom: 16px;
}

.loading, .empty {
  color: #888;
  text-align: center;
  padding: 40px;
}

.current-params {
  padding: 16px;
  background: #1e1e1e;
  border-radius: 8px;
}

.current-params h3 {
  color: #fff;
  margin-bottom: 8px;
}

.current-params pre {
  color: #888;
  font-size: 12px;
  margin: 0;
}
</style>
```

### 5.5 步骤五：验证参数配置功能

1. **安装插件**：点击"安装插件"按钮
2. **修改参数**：
   - 将题目编号改为 2
   - 关闭"显示提示"开关
   - 选择难度为"中等"
3. **验证参数更新**：
   - 观察下方"当前参数值"是否正确更新
   - 插件展示区是否显示第2题（有效的括号）
   - 提示区域是否隐藏

### 5.6 步骤六：验证插件功能

1. **查看题目**：确认显示的是根据参数选择的题目
2. **查看计时器**：确认计时器正在运行
3. **展开提示**：点击"提示"区域展开（如果 showHints 为 true）
4. **开始编码**：点击"开始编码"按钮
5. **完成打卡**：点击"完成打卡"按钮
6. **验证完成事件**：确认弹出完成提示，显示用时

### 5.7 步骤七：测试不同参数组合

| 测试场景 | questionId | showHints | difficulty | 预期结果 |
|----------|------------|-----------|------------|----------|
| 默认参数 | 1 | true | all | 显示第1题，有提示 |
| 第2题无提示 | 2 | false | all | 显示第2题，无提示区域 |
| 第3题困难 | 3 | true | hard | 显示第3题，有提示，困难标签 |
| 边界值-最小 | 1 | true | all | 显示第1题 |
| 边界值-最大 | 3 | true | all | 显示第3题 |
| 超出范围 | 4 | true | all | 显示第1题（默认回退） |

### 5.8 步骤八：卸载插件

1. 点击"卸载插件"按钮
2. 验证：
   - 插件组件消失
   - 已安装插件列表为空
   - 参数配置区域消失

## 五、测试预期结果

| 测试项 | 预期结果 |
|--------|----------|
| 插件上传 | 返回 201 Created，数据库新增记录 |
| 插件下载 | 返回 200 OK，返回包含 params 和 data 的完整插件数据 |
| 插件安装 | 组件成功挂载，控制台输出挂载日志 |
| 参数配置 | 参数配置组件正确渲染，支持数字/布尔/选择类型 |
| 参数更新 | 修改参数后插件内容实时更新 |
| 题目切换 | questionId 改变后显示对应题目 |
| 提示开关 | showHints 改变后提示区域显示/隐藏 |
| 任务完成 | 点击完成后触发 complete 事件，显示用时 |
| 插件卸载 | 组件成功卸载，资源被清理 |

## 七、参数类型测试矩阵

### 7.1 Number 类型参数

| 测试项 | 输入值 | 预期行为 |
|--------|--------|----------|
| 正常值 | 2 | 正常设置，显示第2题 |
| 最小值 | 1 | 正常设置，显示第1题 |
| 最大值 | 3 | 正常设置，显示第3题 |
| 小于最小值 | 0 | 输入框阻止或回退到最小值 |
| 大于最大值 | 4 | 输入框阻止或回退到最大值 |
| 非数字 | "abc" | 输入框阻止或转为 NaN |

### 7.2 Boolean 类型参数

| 测试项 | 输入值 | 预期行为 |
|--------|--------|----------|
| 开启 | true | 显示提示区域 |
| 关闭 | false | 隐藏提示区域 |
| 默认值 | - | 默认为 true |

### 7.3 Select 类型参数

| 测试项 | 输入值 | 预期行为 |
|--------|--------|----------|
| 全部 | "all" | 显示所有题目（用于筛选场景） |
| 简单 | "easy" | 筛选简单题目 |
| 中等 | "medium" | 筛选中等题目 |
| 困难 | "hard" | 筛选困难题目 |

## 八、测试注意事项

1. **参数类型安全**：确保参数类型与定义一致
2. **默认值处理**：未配置参数时使用默认值
3. **边界值验证**：数字类型参数需要验证 min/max
4. **参数持久化**：参数配置应随计划保存
5. **实时更新**：参数修改后插件应实时响应
6. **错误处理**：无效参数应有降级处理

## 九、问题排查

### 常见错误及解决方案

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| 参数不生效 | 参数未正确传递给组件 | 检查 props 绑定 |
| 类型错误 | 参数类型与定义不匹配 | 添加类型转换 |
| 默认值未应用 | 初始化逻辑缺失 | 在组件 mounted 中设置默认值 |
| 参数更新不响应 | 未使用响应式数据 | 使用 ref/reactive |
| 边界值异常 | 未做 min/max 校验 | 添加参数校验逻辑 |

### 日志查看

```bash
# 后端日志
tail -f backend/logs/application.log

# 前端控制台
# 打开浏览器开发者工具 (F12) -> Console
# 查看参数更新日志和插件挂载日志
```

## 十、扩展测试场景

### 10.1 参数持久化测试

1. 配置参数后保存计划
2. 关闭应用
3. 重新打开应用
4. 验证参数是否恢复

### 10.2 多实例测试

1. 在同一计划中添加多个相同插件节点
2. 为每个节点配置不同参数
3. 验证各节点独立运行

### 10.3 参数校验测试

1. 尝试输入非法参数值
2. 验证校验提示
3. 验证降级处理
