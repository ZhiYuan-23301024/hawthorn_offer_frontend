# 插件开发与上传指南

欢迎来到 Hawthorn Offer 插件平台！本文档将指导你如何创建、测试并上传自己的打卡任务插件。

---

## 目录

1. [快速开始](#1-快速开始)
2. [插件代码编写](#2-插件代码编写)
3. [插件参数定义](#3-插件参数定义)
4. [API 接口参考](#4-api-接口参考)
5. [上传插件](#5-上传插件)
6. [测试与调试](#6-测试与调试)
7. [完整示例](#7-完整示例)

---

## 1. 快速开始

### 1.1 什么是打卡任务插件？

一个打卡任务插件就是一个**自包含的 Vue 组件**，它会被用户下载到本地，嵌入到打卡计划中执行。插件可以包含：

- **自己的 UI 界面**（HTML + CSS）
- **自己的业务逻辑**（JavaScript）
- **可配置的参数**（用户设置后影响插件行为）

### 1.2 插件生命周期

```
开发者编写插件代码 → 上传到平台 → 用户浏览插件商店 → 下载安装 → 拖入打卡计划 → 配置参数 → 执行打卡
```

### 1.3 你需要准备什么

- 一个 **JavaScript 文件**（插件代码，Vue Options API 组件）
- 一组 **参数定义**（可选，如果你的插件需要用户配置）
- 一个 **插件 ID**（全局唯一，建议使用 `kebab-case` 命名，如 `daily-code-challenge`）

---

## 2. 插件代码编写

### 2.1 基本结构

插件代码是一个**标准的 Vue Options API 组件**，导出为 `export default`。它通过 `params` prop 接收用户配置的参数。

```javascript
// my-plugin.js

// 1. 定义模板（HTML 结构）
const template = `
  <div class="my-plugin">
    <h2>我的打卡任务</h2>
    <p>{{ message }}</p>
    <button @click="completeTask">完成打卡</button>
    <div v-if="completed">✅ 已完成！</div>
  </div>
`

// 2. 定义样式（CSS）
const style = `
  <style>
    .my-plugin {
      padding: 24px;
      background: #1e1e1e;
      border-radius: 12px;
      color: #fff;
    }
    .my-plugin h2 { margin-bottom: 16px; }
    .my-plugin button {
      padding: 12px 24px;
      background: #4caf50;
      border: none;
      border-radius: 8px;
      color: #fff;
      cursor: pointer;
      font-size: 16px;
    }
    .my-plugin button:hover { background: #388e3c; }
  </style>
`

// 3. 导出组件
export default {
  name: 'MyPlugin',

  props: {
    params: {
      type: Object,
      default: () => ({})
    }
  },

  data() {
    return {
      message: '开始今天的任务吧！',
      completed: false,
      timer: null,
      elapsedTime: 0
    }
  },

  mounted() {
    console.log('插件已加载，参数:', this.params)
    this.startTimer()
  },

  beforeUnmount() {
    if (this.timer) clearInterval(this.timer)
  },

  methods: {
    startTimer() {
      this.timer = setInterval(() => { this.elapsedTime++ }, 1000)
    },

    formatTime(seconds) {
      const m = Math.floor(seconds / 60)
      const s = seconds % 60
      return `${m.toString().padStart(2, '0')}:${s.toString().padStart(2, '0')}`
    },

    completeTask() {
      this.completed = true
      if (this.timer) clearInterval(this.timer)
      this.$emit('complete', { time: this.elapsedTime })
    }
  },

  template,
  style
}
```

### 2.2 组件规范

| 要求 | 说明 |
|------|------|
| **导出方式** | 必须 `export default` |
| **Props** | 系统会传入 `params` (Object)，包含用户配置的参数值 |
| **事件** | 可触发 `complete` 事件通知系统任务完成 |
| **生命周期** | 支持 `mounted`、`beforeUnmount` 等标准 Vue 生命周期 |
| **API 风格** | 必须使用 **Options API**（`data`、`methods`、`computed` 等），不支持 `<script setup>` Composition API |

### 2.3 接收参数

用户在计划中配置的参数会通过 `props.params` 传入：

```javascript
export default {
  props: {
    params: {
      type: Object,
      default: () => ({})
    }
  },

  computed: {
    // 从 params 中读取参数，提供默认值
    questionCount() {
      return this.params.questionCount || 10
    },
    difficulty() {
      return this.params.difficulty || 'easy'
    }
  },

  mounted() {
    console.log('收到参数:', this.params)
    // 根据参数初始化插件行为
    this.loadQuestions(this.params.questionId || 1)
  }
}
```

### 2.4 代码安全限制

- 代码大小上限：**100 KB**
- 禁止使用的模式（开发阶段暂不强制，但建议避免）：
  - `eval()`
  - `new Function()`
  - `document.write()`
  - 直接操作 `window.location`
  - 直接操作 `localStorage`

---

## 3. 插件参数定义

如果你的插件需要用户配置，你需要定义参数列表。系统支持 4 种参数类型。

### 3.1 参数类型一览

#### number — 数字输入

```json
{
  "name": "题目数量",
  "key": "questionCount",
  "type": "number",
  "default": 10,
  "min": 1,
  "max": 100,
  "placeholder": "输入题目数量"
}
```

#### string — 文本输入

```json
{
  "name": "目标网址",
  "key": "targetUrl",
  "type": "string",
  "default": "https://example.com",
  "placeholder": "输入网址"
}
```

#### boolean — 开关切换

```json
{
  "name": "显示提示",
  "key": "showHints",
  "type": "boolean",
  "default": true
}
```

#### select — 下拉选择

```json
{
  "name": "难度筛选",
  "key": "difficulty",
  "type": "select",
  "default": "all",
  "options": [
    { "label": "全部", "value": "all" },
    { "label": "简单", "value": "easy" },
    { "label": "中等", "value": "medium" },
    { "label": "困难", "value": "hard" }
  ]
}
```

### 3.2 参数字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | 是 | 参数显示名称，用户可见 |
| `key` | string | 是 | 参数键名，插件代码中通过 `this.params[key]` 读取 |
| `type` | string | 是 | 参数类型：`number` / `string` / `boolean` / `select` |
| `default` | any | 是 | 默认值 |
| `options` | array | 仅 select | 选项列表，每项含 `label` 和 `value` |
| `min` | number | 仅 number | 最小值 |
| `max` | number | 仅 number | 最大值 |
| `placeholder` | string | 否 | 输入框占位提示文字 |

---

## 4. API 接口参考

所有接口基础路径：`http://localhost:8080/api`

### 4.1 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/plugins` | 获取插件列表（已发布） |
| `GET` | `/plugins/{id}` | 获取插件详情 |
| `GET` | `/plugins/{id}/manifest` | 获取插件清单 |
| `GET` | `/plugins/{id}/download` | 下载插件代码 |
| `POST` | `/plugins` | **上传插件** |
| `PUT` | `/plugins/{id}` | 更新插件 |
| `DELETE` | `/plugins/{id}` | 删除插件 |
| `POST` | `/plugins/{id}/install` | 安装插件 |
| `POST` | `/plugins/{id}/uninstall` | 卸载插件 |

### 4.2 获取插件列表

**请求：**

```bash
GET /api/plugins?page=0&size=20&category=编程
```

**响应：**

```json
{
  "records": [
    {
      "id": "test-quiz-task",
      "name": "编程刷题测试插件",
      "version": "1.0.0",
      "description": "一个带参数的编程刷题测试插件",
      "author": "Test Author",
      "category": "编程",
      "downloads": 1523,
      "status": "PUBLISHED",
      "params": [
        {
          "name": "题目编号",
          "key": "questionId",
          "type": "number",
          "default": 1,
          "min": 1,
          "max": 3
        }
      ],
      "createdAt": "2024-01-01T00:00:00",
      "updatedAt": "2024-01-01T00:00:00"
    }
  ],
  "total": 1,
  "size": 20,
  "current": 0
}
```

### 4.3 获取插件清单

**请求：**

```bash
GET /api/plugins/{id}/manifest
```

**响应：**

```json
{
  "id": "test-quiz-task",
  "name": "编程刷题测试插件",
  "version": "1.0.0",
  "description": "一个带参数的编程刷题测试插件",
  "author": "Test Author",
  "category": "编程",
  "dependencies": [],
  "entry": "index.js",
  "params": [
    {
      "name": "题目编号",
      "key": "questionId",
      "type": "number",
      "default": 1,
      "min": 1,
      "max": 3
    }
  ],
  "createdAt": "2024-01-01T00:00:00",
  "updatedAt": "2024-01-01T00:00:00"
}
```

### 4.4 下载插件代码

**请求：**

```bash
GET /api/plugins/{id}/download
```

**响应：** 返回 `application/javascript` 类型的纯文本，即上传时提供的 `code` 字段内容。

---

## 5. 上传插件

### 5.1 上传接口

```bash
POST /api/plugins
Content-Type: application/json
```

### 5.2 请求体字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | **是** | 插件唯一标识，全局不可重复，建议 `kebab-case` |
| `name` | string | **是** | 插件名称 |
| `version` | string | **是** | 版本号，遵循语义化版本（如 `1.0.0`） |
| `code` | string | **是** | 插件完整 JavaScript 代码 |
| `description` | string | 否 | 插件描述 |
| `author` | string | 否 | 作者名称 |
| `category` | string | 否 | 分类：编程 / 学习 / 健康 / 习惯 / 其他 |
| `params` | array | 否 | 参数定义列表（格式见第 3 节） |
| `manifest` | string | 否 | 完整 manifest JSON 字符串（如提供则覆盖自动生成的） |

### 5.3 上传方式一：使用 params 字段（推荐）

```bash
curl -X POST http://localhost:8080/api/plugins \
  -H "Content-Type: application/json" \
  -d '{
    "id": "my-daily-reading",
    "name": "每日阅读打卡",
    "version": "1.0.0",
    "description": "每天阅读技术书籍并记录心得",
    "author": "your-name",
    "category": "学习",
    "params": [
      {
        "name": "目标页数",
        "key": "targetPages",
        "type": "number",
        "default": 30,
        "min": 1,
        "max": 500,
        "placeholder": "每天阅读页数"
      },
      {
        "name": "需要笔记",
        "key": "requireNotes",
        "type": "boolean",
        "default": true
      }
    ],
    "code": "const template = `...`; const style = `...`; export default { ... }"
  }'
```

**说明：** 系统会根据 `id`、`name`、`version`、`params` 等字段自动生成 manifest JSON 存入数据库。这是推荐方式。

### 5.4 上传方式二：使用 manifest 字段

如果你需要更精细地控制 manifest 内容，可以直接提供完整的 manifest JSON 字符串：

```bash
curl -X POST http://localhost:8080/api/plugins \
  -H "Content-Type: application/json" \
  -d '{
    "id": "my-daily-reading",
    "name": "每日阅读打卡",
    "version": "1.0.0",
    "code": "const template = `...`; export default { ... }",
    "manifest": "{\"id\":\"my-daily-reading\",\"name\":\"每日阅读打卡\",\"version\":\"1.0.0\",\"description\":\"...\",\"params\":[...]}"
  }'
```

### 5.5 更新插件

使用 `PUT` 方法更新已存在的插件：

```bash
curl -X PUT http://localhost:8080/api/plugins/my-daily-reading \
  -H "Content-Type: application/json" \
  -d '{
    "id": "my-daily-reading",
    "name": "每日阅读打卡 v2",
    "version": "2.0.0",
    "description": "更新了 UI 和更多功能",
    "author": "your-name",
    "category": "学习",
    "params": [ ... ],
    "code": "const template = `...新的代码...`; export default { ... }"
  }'
```

> **注意：** 更新插件后，状态会重置为 `REVIEW`（待审核），需要管理员审核通过后才能发布。

### 5.6 删除插件

```bash
curl -X DELETE http://localhost:8080/api/plugins/my-daily-reading
```

---

## 6. 测试与调试

### 6.1 本地测试流程

1. **上传插件到后端**

   ```bash
   curl -X POST http://localhost:8080/api/plugins \
     -H "Content-Type: application/json" \
     -d @my-plugin.json
   ```

2. **验证上传成功**

   ```bash
   curl http://localhost:8080/api/plugins/my-plugin-id
   ```

   应返回完整的插件信息，包含 `params` 字段。

3. **验证 manifest 正确**

   ```bash
   curl http://localhost:8080/api/plugins/my-plugin-id/manifest
   ```

   确认 `params` 数组与预期一致。

4. **验证代码可下载**

   ```bash
   curl http://localhost:8080/api/plugins/my-plugin-id/download
   ```

   应返回你上传的 JavaScript 代码原文。

5. **在前端商店中安装测试**

   打开 Hawthorn Offer 应用 → 打卡面板 → 任务商店 → 找到你的插件 → 点击安装

6. **在打卡计划中测试**

   创建打卡计划 → 拖入你的插件 → 如果有参数，点击"设置"配置参数 → 执行打卡

### 6.2 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 上传返回 400 | `id`/`name`/`version`/`code` 缺少或为空 | 确保所有必填字段都有值 |
| 上传成功但前端看不到 | 插件状态为 `REVIEW` 而非 `PUBLISHED` | 联系管理员审核发布 |
| 参数设置按钮不显示 | 上传时未提供 `params` 字段 | 确认请求体中包含 `params` 数组 |
| 插件安装后不工作 | 代码语法错误或使用了 Composition API | 检查代码语法，确保使用 Options API |
| 代码超过 100KB | 插件代码过大 | 精简代码，或将数据外置 |

---

## 7. 完整示例

下面是一个完整的"每日 LeetCode 刷题"插件示例。

### 7.1 插件代码 (`leetcode-daily.js`)

```javascript
const template = `
  <div class="leetcode-plugin">
    <div class="header">
      <h2>今日 LeetCode 刷题</h2>
      <span :class="'difficulty-' + currentDifficulty">{{ difficultyText }}</span>
    </div>

    <div class="timer">
      <span>⏱ {{ formatTime(elapsedTime) }}</span>
    </div>

    <div class="question" v-if="currentQuestion">
      <h3>{{ currentQuestion.title }}</h3>
      <p>{{ currentQuestion.description }}</p>
      <div class="examples">
        <h4>示例：</h4>
        <pre v-for="ex in currentQuestion.examples">{{ ex }}</pre>
      </div>
    </div>

    <div v-if="showHints && currentQuestion.hints.length" class="hints">
      <h4>💡 提示</h4>
      <ul>
        <li v-for="hint in currentQuestion.hints">{{ hint }}</li>
      </ul>
    </div>

    <div class="actions">
      <button class="btn-code" @click="openIDE">打开 IDE</button>
      <button class="btn-done" @click="completeTask">完成打卡</button>
    </div>

    <div v-if="completed" class="completed">
      ✅ 打卡完成！用时 {{ formatTime(elapsedTime) }}
    </div>
  </div>
`

const style = `
  <style>
    .leetcode-plugin {
      padding: 24px;
      background: #1a1a2e;
      border-radius: 12px;
      color: #eee;
      max-width: 700px;
      margin: 0 auto;
    }
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 20px;
    }
    .header h2 { margin: 0; font-size: 22px; }
    .difficulty-easy { color: #00b894; }
    .difficulty-medium { color: #fdcb6e; }
    .difficulty-hard { color: #e17055; }
    .timer {
      text-align: center;
      padding: 12px;
      background: #16213e;
      border-radius: 8px;
      margin-bottom: 20px;
      font-size: 24px;
      font-family: monospace;
    }
    .question {
      background: #16213e;
      padding: 20px;
      border-radius: 8px;
      margin-bottom: 16px;
    }
    .question h3 { margin-top: 0; color: #74b9ff; }
    .question p { line-height: 1.6; color: #b2bec3; }
    .examples pre {
      background: #0f3460;
      padding: 8px 12px;
      border-radius: 4px;
      font-family: monospace;
      font-size: 13px;
      margin: 4px 0;
    }
    .hints {
      background: #16213e;
      padding: 16px 20px;
      border-radius: 8px;
      margin-bottom: 20px;
      border-left: 3px solid #fdcb6e;
    }
    .hints h4 { margin-top: 0; }
    .hints li { color: #b2bec3; margin: 4px 0; }
    .actions { display: flex; gap: 12px; }
    .btn-code, .btn-done {
      flex: 1;
      padding: 14px 24px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      font-weight: 600;
    }
    .btn-code { background: #0984e3; color: #fff; }
    .btn-code:hover { background: #0767b3; }
    .btn-done { background: #00b894; color: #fff; }
    .btn-done:hover { background: #00997a; }
    .completed {
      margin-top: 20px;
      padding: 16px;
      background: #00695c;
      border-radius: 8px;
      text-align: center;
      font-size: 18px;
    }
  </style>
`

// 预置题目数据
const questions = [
  {
    id: 1, title: '两数之和', difficulty: 'easy',
    description: '给定一个整数数组 nums 和一个目标值 target，请你在该数组中找出和为目标值的那两个整数，并返回它们的下标。',
    examples: ['输入：nums = [2,7,11,15], target = 9', '输出：[0,1]'],
    hints: ['可以使用哈希表优化到 O(n)']
  },
  {
    id: 2, title: '最长回文子串', difficulty: 'medium',
    description: '给你一个字符串 s，找到 s 中最长的回文子串。',
    examples: ['输入：s = "babad"', '输出："bab"'],
    hints: ['考虑中心扩展法', '注意奇偶长度的区别']
  },
  {
    id: 3, title: '接雨水', difficulty: 'hard',
    description: '给定 n 个非负整数表示每个宽度为 1 的柱子的高度图，计算按此排列的柱子下雨之后能接多少雨水。',
    examples: ['输入：height = [0,1,0,2,1,0,1,3,2,1,2,1]', '输出：6'],
    hints: ['双指针法', '维护左右最大高度']
  }
]

export default {
  name: 'LeetCodeDaily',
  props: {
    params: { type: Object, default: () => ({}) }
  },
  data() {
    return {
      elapsedTime: 0,
      completed: false,
      timer: null
    }
  },
  computed: {
    questionId() {
      return this.params.questionId || 1
    },
    showHints() {
      return this.params.showHints !== false
    },
    currentQuestion() {
      return questions.find(q => q.id === this.questionId) || questions[0]
    },
    currentDifficulty() {
      return this.currentQuestion.difficulty
    },
    difficultyText() {
      const map = { easy: '简单', medium: '中等', hard: '困难' }
      return map[this.currentDifficulty] || '未知'
    }
  },
  mounted() {
    this.timer = setInterval(() => { this.elapsedTime++ }, 1000)
  },
  beforeUnmount() {
    if (this.timer) clearInterval(this.timer)
  },
  methods: {
    formatTime(s) {
      const m = Math.floor(s / 60), sec = s % 60
      return `${m.toString().padStart(2, '0')}:${sec.toString().padStart(2, '0')}`
    },
    openIDE() {
      window.open('https://leetcode.cn/problemset/all/', '_blank')
    },
    completeTask() {
      this.completed = true
      if (this.timer) clearInterval(this.timer)
      this.$emit('complete', { questionId: this.questionId, time: this.elapsedTime })
    }
  },
  template,
  style
}
```

### 7.2 上传命令

将上述代码保存为 `leetcode-daily.js`，然后执行：

```bash
curl -X POST http://localhost:8080/api/plugins \
  -H "Content-Type: application/json" \
  -d '{
    "id": "leetcode-daily",
    "name": "每日 LeetCode 刷题",
    "version": "1.0.0",
    "description": "每天刷一道 LeetCode 题目，支持选择题目编号和难度",
    "author": "your-name",
    "category": "编程",
    "params": [
      {
        "name": "题目编号",
        "key": "questionId",
        "type": "number",
        "default": 1,
        "min": 1,
        "max": 3,
        "placeholder": "输入 1-3"
      },
      {
        "name": "显示提示",
        "key": "showHints",
        "type": "boolean",
        "default": true
      }
    ],
    "code": "'"$(cat leetcode-daily.js | sed "s/'/'\\\\''/g")"'"
  }'
```

> **提示：** 如果 curl 命令中内联代码过长不方便，可以先将请求体写入 JSON 文件，再用 `curl -d @request.json` 方式上传。

---

## 附录：状态说明

| 状态 | 含义 |
|------|------|
| `DRAFT` | 草稿，未提交 |
| `REVIEW` | 待审核（新上传或更新后的默认状态） |
| `PUBLISHED` | 已发布，用户可见和下载 |
| `DISABLED` | 已禁用，不可见 |

> 新上传的插件状态为 `REVIEW`。如需立即可见，请联系管理员将状态改为 `PUBLISHED`。

---

如有问题，欢迎在项目中提交 Issue 或联系开发团队。